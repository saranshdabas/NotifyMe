{
  "name": "notifyme",
  "version": "1.0.0",
  "description": "A vaccine notifier to get latest results for the available slots in India",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:start": "clear; nodemon --exec babel-node index.js",
    "clean": "rm -rf build && mkdir build",
    "build-server": "babel-node -d ./build ./server -s",
    "build": "babel-node index.js --out-file index-compiled.js",
    "start": "node ./build/index.js"
  },
  "author": "Saransh",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "googleapis": "^73.0.0",
    "mongoose": "^5.12.7",
    "mongoose-unique-validator": "^2.0.3",
    "node-schedule": "^2.0.0",
    "nodemailer": "^6.6.0"
  },
  "devDependencies": {
    "@babel/core": "^7.14.0",
    "@babel/node": "^7.13.13",
    "@babel/preset-env": "^7.14.1",
    "babel-loader": "^8.2.2",
    "nodemon": "^2.0.7"
  }
}
